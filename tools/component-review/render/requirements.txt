# Core (2D renders, PNGs, diff overlay, KiCad 10 embedded models)
cairosvg>=2.7        # SVG -> PNG (needs the system cairo library: libcairo2 on Debian/Ubuntu)
pillow>=10           # diff.png
numpy>=1.24          # diff.png
zstandard>=0.22      # decode KiCad 10 (embedded_files) models

# Optional: server-side GLB + 3D preview PNGs (skipped with a warning when missing, or use --no-3d).
# Contract Addendum 2 moves STEP tessellation to the browser, so these are nice-to-have only.
trimesh>=4.0
shapely>=2.0
mapbox_earcut>=1.0
cascadio>=0.1        # OpenCASCADE STEP tessellation, statically linked (no libGL needed)
